Aggressively delegate work using subagents.

Use Supabase MCP and the CLIs `gh` (GitHub), `vercel` (Vercel), `posthog-cli` (PostHog), and `playwright-cli` (all browser tasks). Never print credentials.
